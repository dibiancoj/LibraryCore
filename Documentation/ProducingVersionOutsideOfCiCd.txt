1. Increment the version in the assembly
2. dotnet pack --configuration Release <-- (at the solution level which will do it for all 3. Or you can do it at the project level)
3. dotnet nuget push "bin/Release/LibraryCore.Core.1.0.1.nupkg" --source "https://nuget.pkg.github.com/dibiancoj/index.json" --api-key {{personal access token}}   <-- make sure you adjust the version nmber in the 1.0.0.nupkg
